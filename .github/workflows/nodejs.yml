name: Deploy VSCode extension

on:
  create:
    tags:
      - v*

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [12.x]

    steps:
      - run: echo json-sorter-1${{ github.ref }}
      - run: echo json-sorter-2${{ github.head_ref }}
      - run: echo json-sorter-3${{ github.base_ref }}
      - run: echo json-sorter-6${GITHUB_EVENT_NAME}
      # - uses: actions/checkout@v2
      # - name: Use Node.js ${{ matrix.node-version }}
      # uses: actions/setup-node@v1
      # with:
      # node-version: ${{ matrix.node-version }}
      # - run: yarn
      # - run: yarn deploy -p ${{ secrets.VSCE_TOKEN }}
      # - run: yarn package
      # - uses: actions/upload-artifact@v1
      # with:
      # name: json-sorter-${{ github.ref }}
      # path: json-sorter-${{ github.ref }}.vsix
